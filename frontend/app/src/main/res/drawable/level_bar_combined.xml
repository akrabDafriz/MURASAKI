<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background layer -->
    <item
        android:drawable="@drawable/level_bar_bg" />

    <!-- Progress layer -->
    <item android:id="@android:id/progress">
        <clip
            android:drawable="@drawable/level_bar_progress" />
    </item>
</layer-list>
